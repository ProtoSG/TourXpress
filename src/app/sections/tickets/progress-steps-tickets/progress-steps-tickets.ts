import { Component } from '@angular/core';
import { ProgressSteps, SectionContainer } from '@components/UI';

@Component({
  selector: 'app-progress-steps-tickets',
  imports: [SectionContainer, ProgressSteps],
  templateUrl: './progress-steps-tickets.html',
  styleUrl: './progress-steps-tickets.scss'
})
export class ProgressStepsTickets {

}
