import { Component } from '@angular/core';
import { BgContainer, Header, SectionContainer } from '@components/UI';

@Component({
  selector: 'app-header-payment',
  imports: [BgContainer, Header, SectionContainer],
  templateUrl: './header-payment.html',
  styleUrl: './header-payment.scss'
})
export class HeaderPayment {

}
