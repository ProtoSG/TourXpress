import { Component } from '@angular/core';
import { ProgressSteps, SectionContainer } from '@components/UI';

@Component({
  selector: 'app-progress-steps-payment',
  imports: [SectionContainer, ProgressSteps],
  templateUrl: './progress-steps-payment.html',
  styleUrl: './progress-steps-payment.scss'
})
export class ProgressStepsPayment {

}
