import { Component } from '@angular/core';
import { ProgressSteps, SectionContainer } from '@components/UI';

@Component({
  selector: 'app-progress-steps-seats',
  imports: [SectionContainer, ProgressSteps],
  templateUrl: './progress-steps-seats.html',
  styleUrl: './progress-steps-seats.scss'
})
export class ProgressStepsSeats {

}
