import { Component } from '@angular/core';
import { BgContainer, Header, SectionContainer } from '@components/UI';

@Component({
  selector: 'app-header-seats',
  imports: [BgContainer, Header, SectionContainer],
  templateUrl: './header-seats.html',
  styleUrl: './header-seats.scss'
})
export class HeaderSeats {

}
